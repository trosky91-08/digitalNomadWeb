<section id="banner" class="banner">
  <figure>
    <img src="/bannerMain.jpg" alt="imagen-guatape" />
  </figure>
  <div class="descripcion">
    <h1>Digital Nomads Association Colombia</h1>
    <h5>Connect, Guide and Inspire</h5>
  </div>
</section>

<style>
  .banner {
    position: relative;
    width: 100%;
    height: 100vh;
    gap: 10px;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    box-sizing: border-box;
    & figure {
      width: 100%;
      & img {
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        object-fit: cover;
        filter: grayscale(10%);
      }
    }
    & .descripcion {
      position: absolute;
      text-align: center;
      & h1 {
        font-size: var(--size-h1);
        font-family: var(--first-font);
        color: var(--third-color);
      }
      & h5 {
        font-family: var(--second-font);
        color: var(--third-color);
        font-size: var(--font-size-p);
      }
    }
  }

  @media (width<1100px) {
    .banner {
      height: 50vh;
    }
  }
  @media (width<830px) {
    .banner {
      height: 100vh;
      & .descripcion {
        & h1 {
          font-size: 40px;
        }
        & h5 {
          margin-top: 2rem;
          font-size: 20px;
        }
      }
    }
  }
</style>
